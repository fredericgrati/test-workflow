machine:
  services:
    - docker

test:
  override:
    - sudo pip install --upgrade docker-compose
    - cd app
    - docker-compose run -d --no-deps web
    - mocha

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY